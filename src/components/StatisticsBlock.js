import React from 'react';

const StatisticsBlock = ({ stats }) => {
  return (
    <div className="statistics-block">
      {/* Display statistics */}
    </div>
  );
}

export default StatisticsBlock;
